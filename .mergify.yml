defaults:
  actions:
    backport:
      assignees:
        - "{{ author }}"

queue_rules:
  - name: default
    conditions:
      - "#approved-reviews-by>1"

pull_request_rules:
  - name: Automatic merge on approval to the main branch
    conditions:
      - "#approved-reviews-by>=1"
      - base=main
      - label=A:automerge
    actions:
      queue:
        name: default
      merge:
        method: squash
<<<<<<< HEAD
        commit_message_template: |
          {{ title }} (#{{ number }})
          {{ body }}
=======
        commit_message_format:
          title: pr-title
          body: pr-body
  - name: Backport patches to the release/v27.x branch
    conditions:
      - base=main
      - label=A:backport/v27.x
    actions:
      backport:
        branches:
          - release/v27.x
>>>>>>> b6e248e (ci(mergify): upgrade configuration to current format (#4074))

  - name: Backport patches to the release/v26.x branch
    conditions:
      - base=main
      - label=A:backport/v26.x
    actions:
      backport:
        branches:
          - release/v26.x
